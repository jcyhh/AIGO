# template-react 实战反馈

## 用途

本项目同时承担 `template-react` 的实战验证任务。在实际业务开发中发现适合复用到模板的规范、能力、缺陷修复或体验优化时，统一记录在本文档中，暂不因为模板演进阻塞当前项目。

模板仓库：<https://github.com/jcyhh/template-react.git>

## 记录原则

- 只记录具有通用价值、适合多个 React 项目复用的内容。
- 当前项目特有的业务需求不回灌模板。
- 每条建议必须包含实战场景、发现的问题、建议方案和验证结果。
- 优先记录事实和复现方式，避免只有结论没有依据。
- 模板更新由用户集中安排；未经确认，不直接修改模板仓库。
- 当前业务开发优先，不因等待模板更新而暂停。

## 状态说明

- `待评估`：已从实战发现，尚未决定是否回灌。
- `计划回灌`：已确认适合模板，等待模板维护任务处理。
- `已回灌`：已在模板仓库完成。
- `不回灌`：确认只适用于当前项目或收益不足。

## 反馈清单

### TRF-001：复制模板后缺少本地环境文件

- 状态：待评估
- 实战场景：从模板仓库复制代码到一个全新的业务项目后安装依赖并运行测试。
- 发现的问题：`.env.development` 和 `.env.production` 按约定不被 Git 跟踪，因此新项目首次运行时不存在；环境变量约定测试会因文件缺失而失败，构建时也会提示 `VITE_APP_NAME` 未定义。
- 建议方案：在模板 README 的初始化步骤中明确要求基于 `.env.example` 创建两份本地环境文件；也可以提供一个 pnpm 初始化脚本完成复制，但不得覆盖已经存在的环境文件。
- 当前项目处理：已创建 `.env.development` 和 `.env.production`，字段与 `.env.example` 一致。
- 验证结果：`tests/env-convention.test.mjs` 的 3 项测试全部通过，两份实际环境文件均被 `.gitignore` 忽略。

### TRF-002：全新复制后缺少 VS Code 工作区设置

- 状态：计划回灌
- 实战场景：从模板仓库复制代码后直接运行完整测试。
- 发现的问题：`tests/vscode-settings.test.mjs` 强制要求 `.vscode/settings.json` 存在且内容完全符合预设，但该配置只服务 VS Code 或兼容编辑器，不影响项目运行、构建和业务行为。未使用 VS Code 的开发者不需要该文件；同时 `.gitignore` 默认忽略 `.vscode/*`，只放行了 `extensions.json`，导致全新复制后完整测试失败。模板测试把可选的编辑器偏好错误地当成了项目必需契约，约束过严。
- 建议方案：删除测试中对 `.vscode/settings.json` 存在性和具体内容的强制校验。可以继续将该文件作为可选的开箱体验提交到模板，并在 `.gitignore` 中明确放行；也可以只在 README 中提供配置示例。无论采用哪种方式，编辑器配置都不应阻塞项目测试。
- 当前项目处理：已从模板复制 `.vscode/settings.json`，当前 VS Code 用户可以获得 SCSS 类名补全；该文件仍被 Git 忽略。
- 验证结果：文件存在时对应测试通过，但这只能证明编辑器配置符合测试预设，不能证明任何运行时、构建或业务行为，因此该测试不属于必要的项目质量门禁。

### TRF-003：新项目缺少明确的初始化入口和配置清单

- 状态：计划回灌
- 实战场景：开发者从模板复制出一个全新项目，准备开始实际业务开发。
- 发现的问题：模板包含较多 README、模块说明和配置能力，但没有一个面向“新项目第一次启动”的明确入口。更关键的是，现有注释大多只说明配置值是什么，没有形成可交接的决策说明：接手者不知道为什么要配置、什么业务场景应该选哪个值、默认值是否可以直接用于正式项目、配置错误会影响哪些模块，以及哪些值在页面开发期可以留空。模板作者可以依靠开发背景补全这些信息，其他开发者无法可靠判断。信息虽然存在于不同文件中，但缺少按执行顺序组织的初始化流程和配置决策契约。
- 建议方案：在模板顶层 README 增加醒目的“新项目初始化”入口，并提供一份可交接的配置表。每一项至少说明：配置位置、用途、可选值、模板默认值、选择依据、修改阶段、是否必填、留空行为、影响范围和验证方式。至少覆盖项目名称与包信息、`.env.development`/`.env.production`、API 地址、部署路径、首页路由、登录模式、布局模式、DApp 链与合约配置、语言开关、品牌资源和最终验证命令。初始化清单按“立即必改、按项目确认、对接接口时、上线前”分阶段，不能只罗列文件名。可进一步提供只做检查、不覆盖已有值的 `pnpm setup:check`，明确报告仍在使用模板默认值或尚未确认的配置，但不把项目特有值写死在模板中。
- 确定方案：采用 Markdown 驱动的 AI 初始化问卷，不开发交互式脚本。模板新增 `PROJECT_SETUP.md` 定义问题、简短选项说明、是否允许跳过、回填位置和验证方式；`AGENTS.md` 要求 AI 在新项目开发前检查并执行该问卷。AI 一次询问一个配置项，只解释模板语义，不进行技术入门教学、不推荐选项，也不替开发者作决定。所有当前阶段必需配置必须由开发者明确回答，即使答案与模板默认值相同也不能自动确认。API、RPC、合约地址等阶段性配置允许明确选择“跳过”，并记录应补齐的开发阶段。全部回答完成后，AI 先展示汇总，由开发者确认，再统一回填项目配置并验证。
- 固定约束告知：问卷开头应先列出不可选择的项目约束，并要求开发者确认已知晓。包管理器固定使用 pnpm，依赖安装、开发、测试、构建和 lint 都通过 pnpm 执行；不得使用 npm 或 yarn，也不得生成 `package-lock.json` 或 `yarn.lock`。这不是问卷选项，AI 不询问是否改用其他包管理器。现有 `preinstall` 拦截继续保留，用于误操作时快速失败，但不能替代项目初期的主动告知。
- 品牌资源必改项：新项目初始化必须要求开发者提供并替换项目 Logo，不能继续使用模板占位 Logo。Logo 统一使用 PNG 资源并放在 `public/brand/app-logo.png`，公共品牌组件和默认开屏页共用这一份资源；开屏页项目名使用 `APP_CONFIG.name` 文本渲染，不单独制作带项目名的开屏 Logo。问卷需要明确检查文件已替换；如果尚未取得正式资源，可以标记为待补，但初始化状态必须保持未完成或明确列为上线前必改项。
- 初始化状态：问卷完成后生成可提交 Git 的 `PROJECT_SETUP_STATUS.md`。该文件只记录已确认项和待配置项，不写冗长决策过程。已确认项用于区分“项目明确采用该值”和“仍在使用未确认的模板默认值”；待配置项记录跳过内容及应补齐阶段。
- 多语言问卷边界：初始化阶段只确认项目是否启用多语言，不要求立即确定最终语言清单。模板和新项目默认只启用肯定存在的三种语言：简体中文 `zh-Hans`、繁体中文 `zh-Hant` 和英语 `en`。其他语言资源可以继续由模板保留，但不默认加入 `APP_LANGUAGES`；待产品明确目标语言后再按需启用。默认语言不纳入问卷，统一沿用模板规则：开发环境使用简体中文，方便开发；生产环境使用英语，适应常见上线用户群。该规则属于模板明确约定，不需要每个项目重复决策。
- 当前项目处理：由开发协作过程逐项识别并修改，尚无统一的新项目初始化清单。
- 验证结果：本项目首次复制后，开发者无法仅凭顶层入口判断应先创建 env、设置应用名、接口地址及其他公共配置，说明当前文档的信息架构不足以支持顺畅开工。

### TRF-004：公共品牌 Logo 样式不应假设图片为正方形

- 状态：计划回灌
- 实战场景：在项目中用真实品牌 Logo 替换模板占位资源。
- 发现的问题：品牌 Logo 的宽高比例由实际资源决定，不一定是正方形。如果公共品牌组件同时固定宽度和高度，非正方形 Logo 会被拉伸、压缩或裁切，不符合可替换模板资源的设计目标。
- 建议方案：公共 `AppBrand` Logo 默认固定展示宽度并使用 `height: auto` 保持图片原始比例。需要严格限制布局高度时优先使用 `max-height` 和 `object-fit: contain`，不要默认强制正方形尺寸。页面特有尺寸由调用方或项目样式调整。
- 当前项目处理：`AppBrand.scss` 已由开发者调整为 `width: 40px; height: auto;`。
- 验证结果：该写法可以兼容横向、纵向和正方形 Logo，替换资源时无需同步修改固定高度。

### TRF-005：语言配置测试不应锁死项目启用的完整语言清单

- 状态：计划回灌
- 实战场景：新项目根据产品需求增减启用语言。
- 发现的问题：当前 `tests/i18n-config.test.mjs` 把 14 种启用语言及顺序写死。语言范围本质上是项目配置，产品后续增减语言会迫使开发者同步修改模板契约测试，容易把可变配置误判为功能回归。
- 建议方案：测试稳定的多语言模块契约，例如启用语言使用标准 code、每个启用语言能加载公共资源、默认和回退语言存在于启用列表。不硬编码完整语言数量、具体清单和人工维护顺序。模板初始配置仅启用 `zh-Hans`、`zh-Hant`、`en`，其他语言由项目按产品要求增加。
- 当前项目处理：已确认 AIGO 初始只需要简体中文、繁体中文和英语；当前代码仍保留模板原有 14 种启用配置，等待当前项目配置调整时一并修改。
- 验证结果：现有测试可以验证所有当前启用语言具有欢迎文案，但首项测试同时把业务可变语言列表锁死，超出了稳定模块契约范围。

### TRF-006：开发服务器应默认提供局域网访问地址

- 状态：计划回灌
- 实战场景：前端启动静态页面或联调环境后，需要由同一局域网内的后端、测试人员和移动设备访问项目。
- 发现的问题：当前 `pnpm dev` 仅执行 `vite`，开发服务器默认只绑定本机地址。终端只显示 Local 地址，并提示使用 `--host` 才能暴露 Network 地址，其他设备无法直接访问，增加联调和测试沟通成本。
- 建议方案：模板开发命令默认启用 Vite 的局域网监听，例如将 `dev` 设置为 `vite --host 0.0.0.0`，或在 `vite.config.ts` 的开发服务器配置中设置 `server.host`。启动后终端应同时显示 Local 和 Network 地址。该设置只用于开发服务器，不影响生产构建和部署；文档应提醒开发者仅在可信局域网中使用。
- 当前项目处理：尚未修改，当前启动命令仍只提供 Local 地址。
- 验证结果：运行 `pnpm dev` 时 Vite 显示 `Network: use --host to expose`，可以稳定复现。

### TRF-007：项目环境地址统一通过本地 env 配置

- 状态：计划回灌
- 实战场景：开发和部署 DApp/H5 项目时，需要配置公司内部 API 地址、RPC 地址、合约地址以及客户提供的生产参数。
- 发现的问题：如果把公司内部 IP、开发接口地址、RPC 地址等直接写在源码中，它们会随代码提交到 Git，造成环境耦合，也可能在公开仓库中暴露项目内部配置。
- 建议方案：类似 `VITE_BASE_URL`、`VITE_RPC_URL`、`VITE_USDT` 的项目和环境相关值统一放在 `.env.development`、`.env.production` 中，不在业务代码里硬编码；两份实际 env 文件继续保持不被 Git 跟踪，仓库只提交空值和说明完整的 `.env.example`。后续发现新的环境相关地址或项目参数时，优先评估是否应增加 env 配置。
- 必要边界：该设计主要用于环境隔离和避免配置进入 Git，不代表 `VITE_*` 值对浏览器保密；前端运行必需的值在构建后仍属于客户端可见信息。前端项目不存放私钥等真正秘密。
- 当前项目处理：`VITE_BASE_URL`、`VITE_RPC_URL` 和 `VITE_USDT` 保持为空，待对应开发阶段填写。

### TRF-008：项目品牌统一使用 public/brand 下的单一 PNG Logo

- 状态：计划回灌
- 实战场景：新项目替换模板品牌 Logo，并在 Header、Sidebar 等公共品牌区域复用。
- 发现的问题：品牌 Logo 通过类似 `@/assets/layout/headbar/logo.png`、`@/assets/start/splash-logo.png` 的页面深层路径分别维护，替换入口不直观，并产生内容重复的 Header Logo 和开屏 Logo。默认开屏实际只需要“项目 Logo + 居中项目名”，无需维护第二张包含文字的合成图片。
- 约束方案：模板项目 Logo 必须使用 PNG，统一放在 `public/brand/app-logo.png`。公共 `AppBrand` 和默认 `SplashPage` 通过部署路径感知的 public URL 共用该资源；开屏项目名直接渲染 `APP_CONFIG.name`。普通页面业务图片仍放在 `src/assets/<页面语义目录>/`，`public/brand` 只存放初始化必须替换的品牌资源。Logo 引用必须兼容 `/h5/` 等非根目录部署。模板回灌后必须删除旧 `src/assets/layout/headbar/logo.png`、`logo.svg` 和 `src/assets/start/splash-logo.png`，不得保留重复品牌资源。
- 初始化要求：替换 `public/brand/app-logo.png` 是新项目初始化的必做项。AI 问卷需要明确要求开发者提供项目 Logo，并检查 Header、Sidebar 和默认开屏页均引用同一文件。Logo 展示继续遵循固定宽度、`height: auto` 的比例适配规则；`public/favicon.ico` 仍作为另一项品牌资源单独替换。
- 当前项目处理：AIGO 已迁移为 `public/brand/app-logo.png`，`AppBrand` 与 `SplashPage` 共用；开屏页在 Logo 下方显示 `APP_CONFIG.name`。旧 Header PNG/SVG、旧开屏 Logo 及空的 `src/assets/start` 目录已删除。当前 PNG 等待开发者替换为最终 AIGO Logo。
- 验证结果：相关组件和开屏页测试 8 项通过，生产构建和 lint 通过；构建产物只保留一份 public 品牌 Logo，不再生成旧开屏 Logo chunk。

### TRF-009：Vite base 必须与应用部署路径保持一致

- 状态：计划回灌
- 实战场景：应用路由部署在 `/h5/`，并从 `public` 使用项目 Logo 等静态资源。
- 发现的问题：模板的 `APP_CONFIG.routeBase` 和 React Router basename 已设置为 `/h5/`，但 `vite.config.ts` 未配置 `base`，Vite 仍按 `/` 处理开发和构建资源路径。首次迁移 `public/app-logo.png` 后，开发服务器请求 `/h5/app-logo.png` 返回 SPA HTML，而不是 PNG，暴露了配置不一致。
- 建议方案：Vite `base`、应用 `routeBase` 和 Router basename 必须来源一致，至少在模板默认值上统一为 `/h5/`。后续初始化问卷修改部署路径时，AI 必须同步更新并验证三处配置，避免路由可用但静态资源失效。
- 当前项目处理：AIGO 已在 `vite.config.ts` 设置 `base: '/h5/'`，与现有应用和路由配置保持一致。
- 验证结果：修改前 `/h5/app-logo.png` 返回 `text/html`；修改后返回 HTTP 200 和 `Content-Type: image/png`，开发服务器显示的 Local、Network 地址也统一带 `/h5/`。

### TRF-010：未配置开发接口时允许 DApp 签名使用临时登录态

- 状态：计划回灌
- 实战场景：项目处于静态页面开发阶段，尚未提供登录接口，但需要完成钱包连接、签名、进入首页以及后续页面流程。
- 发现的问题：DApp 登录签名后必须等待地址登录接口返回 Token，未配置开发接口时无法建立本地登录态并进入首页。临时直接删除认证流程又会偏离真实的钱包交互路径。
- 规范方案：钱包连接和签名流程保持真实执行。仅当运行于开发环境且 `VITE_BASE_URL` 为空时，签名成功后跳过登录请求并调用 `completeLogin('token')`，用于静态页面开发。开发环境一旦配置 `VITE_BASE_URL`，立即恢复正常地址登录接口请求。
- 生产边界：生产环境始终请求登录接口，不使用临时 Token。`.env.production` 的 `VITE_BASE_URL` 强制保持为空，Axios 使用相对 `/api/...` 地址请求当前网站同源接口；不能填写正式接口域名，也不能把“生产地址为空”误判为“接口尚未接入”。初始化问卷应把它作为固定约束告知，而不是可填写配置项。
- RPC 生产边界：`.env.production` 的 `VITE_RPC_URL` 同样强制保持为空。该变量只配置开发环境的 `DAPP_LOCAL_CHAIN` RPC；生产环境直接使用 `DAPP_PRODUCTION_CHAIN`，不读取此配置。初始化问卷只允许在开发联调阶段询问本地 RPC 地址，不应提供生产 RPC 填写项。
- 当前项目处理：AIGO 已在认证配置中集中判断该规则，DApp 登录流程根据环境和 `VITE_BASE_URL` 自动选择临时登录态或真实接口，无需在对接阶段手动改回代码。
- 验证结果：相关认证测试需覆盖临时分支仅由 `DEV && !VITE_BASE_URL` 触发，同时保留真实 `requestDappLogin()` 和接口返回 Token 的完成流程。

### TRF-011：用 Stylelint 强制禁止重复全局工具样式

- 状态：计划回灌
- 实战场景：页面开发时，全局 SCSS 已生成间距、字号、字重、行高、透明度和动画时长等工具类，但开发者仍可能在页面 SCSS 中重复声明相同具体值，使工具类体系失去约束力。
- 发现的问题：仅在 `AGENTS.md` 记录“优先复用公共类”无法阻止遗漏。普通代码测试也不适合逐个锁定页面样式值，需要独立的样式静态检查门禁。
- 规范方案：引入 Stylelint 和本地自定义规则。`src/styles/**` 允许定义全局工具类；其他 SCSS 文件中，如果声明值已经能映射到现有工具类，则直接报错并给出推荐类名，例如 `margin-top: 24px → mt-24`、`font-size: 36px → size-36`、`font-weight: 600 → bold-6`、`line-height: 40px → lh-40`、`opacity: 0.6 → opc-6`、`animation-delay: 0.3s → ani-delay-3`。没有对应工具类的页面特有值继续允许。
- 执行方式：提供 `pnpm stylelint` 检查全部 `src/**/*.scss`，并将它接入 `pnpm lint`，与 Oxlint 一起作为完成前门禁。违规时应修改 JSX `className`，不通过禁用规则绕过。
- 当前项目处理：AIGO 已添加本地 Stylelint 规则、配置、行为测试和 pnpm 命令。
- 验证结果：规则测试覆盖页面重复声明报错、全局样式目录放行、无对应工具类的页面值放行。首次全项目检测发现 10 处存量违规，已全部迁移到 JSX 工具类；最终 `pnpm stylelint`、完整测试、生产构建和 `pnpm lint` 均通过。

### TRF-012：禁止 TSX inline style 绕过 PostCSS 自适应

- 状态：计划回灌
- 实战场景：项目依赖 PostCSS 将 SCSS 中基于 750 设计稿的 px 转为 vw；如果开发者在 TSX 的 `style` 属性中写数字、px 字符串或动态样式对象，这些值不会经过 PostCSS，导致移动端自适应不一致。
- 强制规则：所有 TSX 中禁止 JSX `style` 属性，不区分静态、动态、数字或字符串写法。动态视觉状态必须在页面或组件私有 SCSS 中预定义语义化状态类，通过 React `className` 条件切换。需要尺寸和颜色的公共组件同样通过全局工具类或私有修饰类表达，不能保留 inline style 逃生口。
- 执行方式：新增 TypeScript AST 静态检查命令 `pnpm lint:tsx-styles`，扫描 `src/**/*.{tsx,jsx}` 的所有 JSX `style` 属性并报出文件、行和列。该检查接入 `pnpm lint`，违规直接失败。
- 当前项目处理：Icon 移除 `style`、`size`、`color` 参数并改用工具类；ContractLoading 的动态延迟改为 SCSS 修饰类；LanguageSwitch 禁用时直接返回 `null`；Picker Showcase 的动态颜色改为私有 tone 修饰类。
- 验证结果：规则测试覆盖对象、px 字符串、变量形式全部报错以及动态 `className` 放行。项目现有 4 处 inline style 已全部迁移；`pnpm lint:tsx-styles` 和 Stylelint 均为 0 违规，完整测试 170 项、生产构建及总 lint 全部通过。

### TRF-013：vh/vw 必须提供 dvh/dvw 动态视口覆盖

- 状态：计划回灌
- 实战场景：移动端浏览器地址栏、工具栏和软键盘会改变实际可视区域，单独使用传统 `vh`、`vw`，尤其在 `calc()` 布局中，可能与当前动态视口不一致。
- 强制规则：私有 SCSS 中每个包含 `vh` 或 `vw` 的声明，必须立即追加同属性、同表达式的 `dvh` 或 `dvw` 版本。传统单位写在前面作为兼容回退，动态单位写在后面覆盖。规则同时覆盖直接尺寸与 `calc()` 等复合表达式。
- TSX 边界：TSX 已禁止 JSX inline style；如果第三方组件参数、常量字符串或模板字符串仍需要使用 `vh`、`vw`，同一文件必须提供等价的 `dvh`、`dvw` 值。TSX AST 检查会报告缺少的动态单位表达式。
- 执行方式：Stylelint 新增 `aigo/require-dynamic-viewport-pair`，`pnpm lint:tsx-styles` 扩展视口字符串配对检查，两者均已包含在 `pnpm lint` 中。
- 当前项目处理：全局 viewport 工具类、Popup 和高度 mixin 原本已经成对；Picker 的 `100vw` 和 Popup Showcase 的 `82vw` 已补充对应 `dvw`。TSX 检查会识别真实 CSS 单位，但不会误报内部已经成对实现的 `vw-100`、`vh-100` 工具类名称。
- 验证结果：规则测试覆盖缺少配对报错、正确顺序放行、错误顺序报错、TSX 字符串配对检查。Stylelint 和 TSX 样式检查均为 0 违规，完整测试 174 项、生产构建和总 lint 全部通过。

### TRF-014：自定义规范门禁只约束一方代码

- 状态：计划回灌
- 实战场景：项目引入 npm 包、复制第三方源码或接入生成代码时，模板自定义的工具类、inline style 和动态视口规则不应强迫开发者修改第三方实现。
- 强制边界：所有自定义 lint 规则只约束模板自身封装和项目新增的一方代码。`node_modules/**`、`src/vendor/**`、`src/third-party/**` 明确排除；复制到仓库的第三方或生成源码应放入后两类目录。项目团队自己编写的第三方库 Wrapper、业务适配层和覆盖样式仍属于一方代码，继续遵守项目规范。
- 实现方式：Stylelint 配置从文件扫描层忽略第三方目录，各本地 Stylelint 插件和 TSX AST 检查器也在规则层显式跳过，形成双重边界，避免单独调用规则时误报。
- 当前项目处理：已新增共享源码范围判断，并应用到重复工具样式、动态视口配对、TSX inline style 和 TSX 视口字符串检查。
- 验证结果：新增测试证明相同违规写法在一方目录会报错，在 `src/vendor`、`src/third-party` 中会放行。完整测试 177 项、生产构建、Stylelint、TSX 样式检查和总 lint 全部通过。

### TRF-015：生产构建必须强制通过规范和测试门禁

- 状态：计划回灌
- 实战场景：项目虽然提供 `pnpm lint` 和 `pnpm test`，但开发者如果不主动执行，仍可能直接构建不符合规范的生产包。
- 强制规则：新增 `pnpm verify` 统一执行 `pnpm lint && pnpm test`；`prebuild` 在包管理器校验后强制运行 `pnpm verify`。只有代码规范、SCSS 工具类、TSX 样式、动态视口和全部测试通过后，`pnpm build` 才会执行 TypeScript 与 Vite 构建。
- 边界说明：该门禁防止标准构建流程被无意绕过；CI 仍应直接执行或依赖 `pnpm build`，并将构建结果设置为正式分支的必需检查。不得提供跳过 verify 的替代生产构建脚本。
- 当前项目处理：AIGO 已新增 `verify` 脚本并接入 `prebuild`，文档和 AI 规则同步说明该门禁。
- 验证结果：已做红绿验证：临时删除 `SplashPage` 的 `100dvh` 后，`pnpm build` 在 `prebuild` 的 Stylelint 阶段退出，且未进入 TypeScript/Vite 构建；恢复后代码检查、177 项测试和生产构建全部通过。

### TRF-016：一级 Layout 品牌区需要按项目设计完成初始化

- 状态：计划回灌
- 实战场景：完成开屏页后进入一级 Layout 公共区域初始化，Header、Sidebar 等位置会复用 `AppBrand`，其 Logo 尺寸、品牌文字尺寸和间距需要与当前项目设计稿一致。
- 初始化要求：AI 初始化清单需要提醒开发者确认一级 Layout 品牌区效果，并明确检查 Logo 展示宽度、等比自适应、品牌名字号、字重和图文间距。模板保留可复用结构和公共工具类用法，但不能把某个项目的设计尺寸当成所有新项目的固定默认值。
- 当前项目处理：AIGO 已由开发者按设计稿手动调整完成；`AppBrand` Logo 为 `width: 60px; height: auto;`，品牌名使用 `ml-10 size-32 bold`。这些是 AIGO 的项目参数，后续修改必须保留开发者已经确认的样式。
- 验证结果：已核对组件实现与设计标注，Logo 宽度为 60px，文字字号为 32px，并继续复用 `public/brand/app-logo.png` 和 `LAYOUT_HEADER_TITLE`。

### TRF-017：新项目初始化必须按 UI 设计确认全局色板

- 状态：计划回灌
- 实战场景：模板提供的通用颜色只能作为结构和占位默认值；进入项目页面开发前，需要依据当前项目 UI 设计统一确定品牌色、状态色、页面背景、卡片背景、按钮文字和输入框占位色等公共颜色。
- 初始化要求：AI 初始化清单需要引导开发者确认是否已经取得 UI 色板。已取得时更新 `src/styles/color.scss`；尚未取得时允许阶段性跳过，但必须保留为页面开发或上线前待办。模板只沉淀语义化变量与工具类结构，不回灌具体项目色值。
- 当前项目处理：AIGO 已由开发者依据 UI 设计手动完成 `color.scss` 调整，包括品牌色、绿/红/蓝/黄色、页面背景、二级 Header、卡片、Box、按钮文字和输入框占位色。后续开发应优先复用这些全局变量及颜色工具类，不在私有样式中重复定义相同公共颜色。
- 验证结果：已核对当前色板文件并保留开发者配置；AIGO 具体色值仅属于当前项目。

### TRF-018：项目主操作按钮与场景专属按钮必须划清边界

- 状态：计划回灌
- 实战场景：不同项目的购买、确认、提交等主操作按钮可能采用纯色、渐变背景或渐变描边，适合在初始化阶段统一配置；但 Header 中的钱包连接或地址按钮即使外观相似，也可能拥有完全不同的设计。
- 初始化要求：初始化清单只确认项目级主操作按钮的文字色、背景、边框、圆角和基础高度，并允许纯色、渐变背景、渐变描边等皮肤。按钮的通栏与内容自适应尺寸结构应和视觉皮肤分离。
- 封装边界：Header 钱包按钮、语言按钮、筛选标签等场景控件不默认继承主操作按钮皮肤，应在所属公共组件或业务组件的私有样式中实现。只有同一种视觉在多个无关区域稳定重复使用后，才评估提升为公共按钮变体，避免过度模板化。
- 当前项目处理：AIGO 的 Header 钱包地址按钮继续由 Header 区域按设计单独调整；购买、确认等主操作按钮复用项目级按钮样式。
- 验证结果：规则边界已通过历史项目设计稿对比确认，暂不因单个 Header 按钮新增全局样式。

### TRF-019：按钮结构与按钮皮肤应分层预置

- 状态：计划回灌
- 实战场景：当前模板已有 `full-button` 和 `auto-button` 两种按钮结构。`full-button` 表示撑满一行的按钮，`auto-button` 表示根据内容宽度自动撑开的按钮。项目初始化时真正变化的是按钮视觉皮肤，例如纯色、渐变背景、渐变背景加渐变边框，后续还可能出现 CSS 难以还原的复杂图片背景按钮。
- 建议方案：模板回灌时不要只提供一个默认主按钮，而是在公共样式中预置“结构 × 皮肤”的组合类。初始至少包括纯色按钮、渐变按钮、渐变背景加渐变边框按钮，并保留继续扩展图片背景按钮等复杂皮肤的空间。初始化清单中让开发者了解这些类型，再按 UI 设计调整对应颜色、渐变、边框、圆角、高度、背景图资源等配置。
- 边界约束：这属于模板未来能力，不应为了验证方案而一次性引入过多按钮变体。AIGO 当前已确认主按钮为蓝色渐变样式，先通过项目级 `--app-btn-bg` 和 `--app-btn-color` 统一当前主按钮皮肤，后续只有当 UI 明确出现渐变描边或图片背景主按钮时，才继续增加对应变体。
- 当前项目处理：AIGO 已将 `full-button` 和 `auto-button` 的默认背景收口到 `--app-btn-bg`，页面主按钮不再重复写同一段渐变。
- 验证结果：已根据当前 AIGO UI 和历史项目按钮风格确认该方案适合回灌模板，但不适合现在提前落到 AIGO 代码中。

### TRF-020：项目规范优先沉淀到 Markdown，代码中减少解释性注释

- 状态：计划回灌
- 实战场景：模板目标是面向 Agent 和 Markdown 开发，人工主要负责审核与测试。大量代码注释会把规范散落在源码里，后续维护时容易和文档脱节，也会诱导人工直接改代码而不是先通过 AI 理解模板约束。
- 建议方案：模板规范、初始化流程、配置含义、封装边界、AI 协作要求优先写入 `AGENTS.md`、组件 README、初始化清单和实战反馈文档。源码中默认不写解释性注释，只有浏览器兼容、异常边界、危险约束或短期 TODO 这类不写会明显增加误改风险的场景，才允许保留极少量注释。
- 执行方式：后续回灌模板时，可以逐步把配置项说明、业务占位说明、组件使用规则迁移到 Markdown；代码里只保留自解释命名和必要边界。清理注释应分批做，避免在当前项目开发阶段产生大面积无业务价值改动。
- 当前项目处理：先记录原则，暂不批量删除 AIGO 源码注释。
- 验证结果：已确认该方向符合“AI 优先、人工审核”的模板目标；当前项目不做额外代码清理。

### TRF-021：一级页面命名词库只作推荐，Sidebar 必须保持全屏高度

- 状态：计划回灌
- 实战场景：模板可以沉淀常见一级页面英文命名，例如 `home`、`user`、`asset`、`swap`、`team`、`order` 等，帮助 AI 在开发者未介入时保持命名统一。但项目实际业务可能有更准确的行业词或产品词，不能把推荐词库变成强制字典。
- 命名规则：一级页面命名以开发者确认优先。开发者没有介入时，AI 按推荐词库和行业语义选择最贴近的英文名。首页无论产品里叫首页、总览、Dashboard 还是其他名称，文件名和路由名都固定使用 `home`，保证全局首页逻辑稳定。
- Sidebar 约束：当项目选择 `sidebar` 菜单模式时，侧边栏内容必须按全屏高度实现，不能因为设计稿露出顶部导航栏或局部页面背景就做成非全屏容器。当前 Popup 组件是全屏覆盖层级，Sidebar 如果漏出 Header，会迫使业务设计反向修改 Popup 封装，破坏公共组件边界。
- 当前项目处理：AIGO 当前 `SidebarMenu` 使用 `app-sidebar-menu vh-100`，符合全屏高度约束；一级页面已按当前业务先落为 `home`、`swap`、`weight`。其中 `weight` 是基于当前“权重”语义的行业化命名选择，后续开发者如确认更准确词汇，可按业务名调整。
- 验证结果：已核对当前 Sidebar 结构，现状符合约束；本次仅记录模板规范，不修改代码。

### TRF-022：公共进度条组件应沉淀为模板基础组件

- 状态：计划回灌
- 实战场景：DApp 页面中经常需要展示额度、进度、比例、任务完成度等横向进度条，视觉样式会随项目 UI 变化，但计算逻辑和基础结构具有通用价值。
- 发现的问题：如果每个页面自己计算百分比并手写进度条宽度，容易出现精度、边界值、动态样式和主题色不一致。项目已禁止 TSX inline style，不能通过 `style={{ width: 'xx%' }}` 绕过 PostCSS 和样式规范。
- 建议方案：模板新增公共 `ProgressBar` 组件，由调用方传入用于计算的 `currentValue` 和 `totalValue`，组件内部统一调用 `calculatePercentage()` 得到 `0-100` 整数百分比。默认 `solid` 变体使用纯色轨道和纯色进度，进度颜色默认走主题色 `--app-color`，轨道默认 `rgba(255, 255, 255, 0.1)`。后续如果项目出现渐变、渐变边框、图片背景等样式，再继续按 `variant` 扩展。
- 当前项目处理：AIGO 已新增 `src/components/ProgressBar`，默认使用原生 `progress` 元素承载动态值，避免 TSX inline style。新增 `--app-progress-color` 和 `--app-progress-bg` 作为主题变量。
- 验证结果：已新增进度条组件测试，覆盖内部计算、默认 solid 类型、主题色变量和无 inline style。

### TRF-023：设计稿页面开发前必须先准备切图资源

- 状态：计划回灌
- 实战场景：根据 Figma 设计稿开发移动端页面时，页面背景、复杂装饰边框、发光效果、图片型按钮、特殊图标等视觉元素很难由 AI 仅靠截图和 CSS 稳定还原。切图边界如果完全交给 AI 判断，容易出现本应使用图片的区域被 CSS 圆形、文字、符号或简化图形顶替，导致页面从第一版就偏离设计稿。
- 发现的问题：当前首页实战中，部分应依赖设计切图或真实图标的区域被临时代码样式应付。即使布局大体可用，细节还原度会明显下降，并且后续人工返工成本高。
- 建议方案：模板新增页面开发前置流程：开发设计稿页面前，先由开发者或设计协作方人工从 Figma 导出页面所需 PNG/SVG/WebP 切图，并放入项目语义化资源目录。AI 开始写页面前必须先检查资源清单和设计稿结构；复杂图片、装饰边框、光效、页面背景、图片型按钮和非常规图标优先使用切图。缺少必需资源时，AI 应暂停实现并列出缺失资源名和建议路径，不得擅自用 CSS、文字、emoji 或临时符号替代。只有纯色、渐变、圆角、普通边框、常规布局和可复用基础组件才默认用代码实现。
- 可访问性边界：装饰性设计切图不强制要求编写 `alt`，后续页面开发不把视觉切图缺少 `alt` 当作阻塞问题；真正承载业务信息或交互含义的图片再按具体场景补语义。
- 资源路径约定：页面专属资源放在 `src/assets/<page-name>/`，按语义命名，例如 `home-bg.png`、`home-card-frame.png`、`token-usdt.svg`。品牌初始化资源继续放在 `public/brand/`，不与页面切图混放。
- 当前项目处理：已将该规则写入 `AGENTS.md`，后续开发新页面前先确认切图资源是否齐全。当前已写出的首页若继续精修，应优先补齐真实设计切图，再替换临时代码图形。
- 验证结果：本次为流程和文档规范更新，后续页面开发时通过人工资源清单、页面实现检查和构建门禁共同验证。

### TRF-024：Figma 页面生成必须先做实现清单再写代码

- 状态：计划回灌
- 实战场景：开发者直接提供 Figma 链接并希望 AI 生成页面时，AI 容易只按视觉结果“摹写”页面，而没有先对齐项目已有封装、控件语义和切图边界。当前首页实战中出现了两个典型问题：本应是输入框的区域先被写成静态 `div`，本应复用 `full-btn` 或 `@include full-button(...)` 的确认按钮被写成了页面私有按钮结构。
- 发现的问题：Figma 设计稿主要表达视觉结构，不会自动告诉 AI 当前项目已经有哪些全局组件、mixin、工具类和封装边界。仅靠 `AGENTS.md` 中“优先复用公共类”的原则仍不够，AI 在页面生成时可能跳过“查已有封装”这一步，导致重复 SCSS、错误语义元素和后续接接口成本增加。
- 建议方案：模板新增强制页面生成前置流程。AI 收到 Figma 链接后，不能直接写页面代码，必须先检查设计稿和项目现状，并输出一份实现清单供开发者确认。清单至少包含：所需切图资源、语义化 UI 元素、可复用组件、可复用样式类、可复用 mixin、页面专属例外和待确认项。AI 在写页面 SCSS 前必须检查 `src/components`、`src/styles`、`src/styles/mixins.scss`、showcase 页面和相近页面实现。
- 按钮约束：通栏主操作按钮默认使用 `full-btn` 或 `@include full-button(...)`；内容自适应按钮默认使用 `auto-btn` 或 `@include auto-button(...)`。只有设计明确需要共享按钮结构无法表达的形状、图片背景或交互时，才允许写页面专属按钮结构，并应在实现清单中说明原因。
- 语义约束：视觉控件写样式前先判定语义。输入区域必须使用 `input` 或 `textarea`，操作使用 `button`，跳转使用路由/导航封装，进度展示优先使用公共进度组件。不得为了静态页面方便把交互控件写成普通 `div`。
- 当前项目处理：已将该流程写入 `AGENTS.md`，并新增文档契约测试，要求 Figma 页面实现流程、按钮复用和语义元素规则继续存在。首页后续精修时应按该流程重新核对确认按钮、输入框、进度条和切图资源。
- 验证结果：本次为规范和反馈记录更新，后续页面实现通过页面前置清单、文档契约测试、lint、测试和构建门禁共同验证。

### TRF-025：静态页面也要实现基础本地交互

- 状态：计划回灌
- 实战场景：项目处于静态页面开发阶段，接口尚未接入，但页面设计稿已经包含 tab、输入框、按钮、弹窗、复制等可交互控件。
- 发现的问题：把“静态页面”理解成“无交互截图”会导致 tab 只画出两个按钮却不能切换，输入区域被写成静态文本，弹窗和展开收起没有最小状态流。这样第一版虽然能看，但后续接接口时还要重写结构和状态，返工成本高。
- 建议方案：静态数据不等于无交互截图。页面开发期可以不请求接口、不接合约，但设计中明确出现的基础本地交互应先用本地 state 和静态数据实现，例如 tab 切换、输入框、展开收起、弹窗开关、复制按钮、领取按钮禁用态等。交互只需要走到前端状态层，不需要提前模拟复杂业务链路。
- 当前项目处理：AIGO 首页协作订单 tab 已改为本地状态切换，并按订单状态过滤静态数据；订单进度区顺序同步按设计调整。
- 验证结果：新增首页结构测试和文档契约测试，要求后续页面保留基础本地交互的实现习惯。

### TRF-026：重复出现的居中弹窗卡片皮肤应沉淀为 Popup 内容主题

- 状态：计划回灌
- 实战场景：存钱罐页面的存入和提取弹窗使用同一类居中卡片视觉：模糊遮罩、渐变背景卡片、渐变边框、顶部标题和右上角关闭按钮。
- 发现的问题：如果每个页面都通过 `contentClassName` 私写完整卡片外壳，会让 Popup 公共封装只剩遮罩能力，后续相同弹窗视觉在购买、确认、领取等场景里反复复制。
- 建议方案：Popup 的预设内容外壳保留默认主题，同时增加 `contentTheme` 这类语义化视觉主题参数。初始至少支持 `default` 和 `gradient-card`，其中 `gradient-card` 复用模板已有 `gradient-card` mixin，颜色和渐变走全局变量配置。页面只写表单、按钮、余额等业务主体，不重复写公共卡片边框与背景。
- 当前项目处理：AIGO 已给 `PopupContentCenter` 和 `PopupContentBottom` 接入 `contentTheme`，并新增 `gradient-card` 公共样式；存钱罐存入/提取弹窗使用该主题。
- 验证结果：新增 Popup 内容主题契约测试，要求主题类型、主题 class、渐变卡片样式和全局变量持续存在。

### TRF-027：项目术语必须形成统一词表并可被测试约束

- 状态：计划回灌
- 实战场景：Figma 节点、历史项目习惯和业务口头描述可能对同一动作使用不同词，例如 Token 取回动作同时出现“提取”和“提现”。
- 发现的问题：如果 AI 完全照搬设计稿文字，页面不同位置会出现不一致文案，后续多语言、接口字段、测试用例和运营口径都会被拖乱。
- 建议方案：项目初始化和页面开发阶段都应维护一份业务术语约束。设计稿与术语表冲突时，源码以术语表为准，并把差异记录到实现说明或反馈文档。对关键禁用词可以增加轻量测试扫描一方 UI 源码，防止后续误写。
- 当前项目处理：AIGO 已确认 Token 取回动作统一叫“提取”，不叫“提现”；`AGENTS.md` 已写入术语规则，并新增源码扫描测试。
- 验证结果：新增术语测试扫描 `src` 下的一方 UI 源码，出现“提现”会直接失败。

### TRF-028：页面内分段 Tab 应沉淀为公共 SegmentedTabs 组件

- 状态：计划回灌
- 实战场景：AIGO 首页“协作订单”的“进行中 / 已完成”切换已经实现了基础本地交互，并按当前选中项过滤静态数据。该控件包含容器背景、绝对定位选中色块、选中色块位置动画、文案颜色过渡和多个按钮项，后续订单、资产、流水、收益明细等页面都可能重复出现。
- 发现的问题：如果每个页面都私写同样的 tab 结构，容易重复写动态选中类、动画色块和 `aria-pressed`，也容易出现有的页面只改背景色、没有滑块动画，交互体验不统一。
- 建议方案：模板新增公共 `SegmentedTabs` 或 `TabSwitch` 组件，采用受控模式：`options`、`value`、`onChange`。组件内部负责按钮语义、选中态、禁用态、选中色块动画和文案颜色过渡。因为项目禁止 TSX inline style，选中色块位置不能用 `style={{ left }}`，应通过 `count` 和 `activeIndex` 修饰类控制，默认支持常见的 2 到 5 项。颜色、背景、圆角、高度、内边距和选中色块背景走全局变量或组件 class 扩展。
- 边界约束：该组件只负责“同级互斥筛选 / 状态切换”，不负责请求分页、列表过滤业务或路由跳转。页面继续自己维护状态和数据过滤；如果是一级菜单或路由导航，应继续使用路由/菜单封装，不用该组件。
- 当前项目处理：AIGO 首页当前仍是页面私有实现，已经证明需要抽公共组件；下一步可以先抽 `SegmentedTabs` 并替换首页订单 tab。
- 验证结果：本次先记录模板反馈，后续实现组件时需补组件契约测试，覆盖受控值、点击回调、选中态 class、无 inline style 和首页接入。

### TRF-029：模板缺少分页组件，接口阶段前应补基础 Pagination

- 状态：计划回灌
- 实战场景：AIGO 当前静态页面已有多个列表：协作订单、闪兑记录、流水明细。现在用静态数组直接渲染，接口阶段接入真实列表后必然需要页码、上一页/下一页、加载状态、空状态和列表刷新策略。
- 发现的问题：模板目前没有公共分页组件。若接口阶段每个页面自己处理分页按钮和边界值，容易出现页码越界、重复请求、禁用态不统一、移动端分页样式不一致。
- 建议方案：模板新增受控 `Pagination` 组件，基础参数包括 `currentPage`、`pageSize`、`total` 或 `totalPages`、`onChange`、`disabled`。默认提供上一页、下一页、页码项和省略号展示，内部负责页码边界裁剪与按钮禁用态。移动端 H5 也应预留 `simple` 或 `load-more` 轻量展示模式，但不要把接口请求、列表数据和滚动加载写进组件内部。
- 边界约束：分页组件只负责展示和发出页码变化，页面或 feature service 负责请求接口、维护 loading、合并列表和错误处理。若项目设计最终采用触底加载或“加载更多”，可以复用同一分页状态模型，但 UI 变体应和页码式分页区分。
- 当前项目处理：AIGO 仍处于静态页面阶段，暂不强行把当前静态列表改成分页；先记录为接口阶段前的公共能力缺口。
- 验证结果：本次先记录模板反馈，后续实现时需补页码计算测试、边界禁用测试、无 inline style 测试和 showcase 示例。

### TRF-030：静态页收尾后应做一次公共视觉原语复盘

- 状态：计划回灌
- 实战场景：AIGO 静态页完成后，多个视觉结构开始重复出现：带左右渐隐线的区块标题、Token 胶囊、金额输入框加“全部”、渐变/半透明记录卡片、列表行分隔线等。
- 发现的问题：这些结构如果在第一个页面就过早抽象，容易把项目专属样式误判成模板能力；但如果完全不复盘，后续页面会继续复制相同 SCSS，公共组件体系也会错过真实项目打磨出来的高频结构。
- 建议方案：每完成一轮静态页面，应做一次“公共视觉原语复盘”。规则是：两个以上无关页面稳定重复、语义一致、业务逻辑轻、样式可通过变量配置时，优先抽公共组件或公共样式；只出现在单页、依赖具体切图、依赖业务字段或只是视觉相似但语义不同的结构，先保留在页面内。
- 当前候选：`SectionTitle` 适合承载左右渐隐线标题；`TokenPill` 适合承载图标 + Token 名称胶囊；金额输入 + 余额 + 全部更适合等接口阶段确认资产选择、精度、最大值规则后再抽；记录卡片先保留页面内，因为 home、swap、weight 的信息结构差异较大。
- 当前项目处理：AIGO 暂不批量抽所有候选，优先考虑先抽首页订单 tab 为公共 `SegmentedTabs`，分页组件作为接口阶段前置能力。
- 验证结果：本次先记录模板反馈，后续每抽一个公共组件都应补 README、showcase 和契约测试。

### TRF-031：项目合约封装应独立于通用 DApp 能力目录

- 状态：计划回灌
- 实战场景：DApp 项目接入合约 ABI 后，需要把页面会调用的合约读写方法封装成稳定的业务方法，同时继续复用模板已有的钱包连接、链配置、合约写入 loading、gas 检查和金额单位转换等通用能力。
- 发现的问题：如果把项目专属 ABI 和业务方法直接塞进 `src/services/dapp`，会污染模板通用 DApp 模块，让 `dapp` 同时承担“钱包基础设施”和“某个业务合约”的职责。后续复制到其他项目时，项目 ABI、地址、方法名和业务 README 都会变成模板噪音。
- 建议方案：模板保留 `src/services/dapp` 作为通用 Web3 基础能力目录；每个具体业务项目把 ABI、合约地址配置和页面语义方法统一放在 `src/services/contracts/`。推荐结构为：`config.ts` 统一读取合约 env 和必填地址校验；`<contractName>.ts` 按合约或业务语义封装 ABI、底层 read/write 方法和页面友好的 action 工厂；`index.ts` 统一导出；`README.md` 记录合约范围、env key、缺失 ABI 项和封装边界。
- 目录约束：合约文件名优先使用合约业务语义，例如 `aigoProject.ts`、`laxProject.ts`；ABI 常量、底层方法和页面 action 均从同一个文件导出。项目特有地址进入 `.env.development` / `.env.production` / `.env.example` 和 `vite-env.d.ts`，其中生产 env 仍遵循项目规则，不因合约接入而写入不该进入生产配置的开发地址。
- 当前项目处理：AIGO 已新增 `src/services/contracts/config.ts`、`aigoProject.ts`、`laxProject.ts`、`index.ts` 和 `README.md`。`AIGOProjectV1` 与 `LAXProject` 的 ABI 和页面会用的方法已在该目录封装；`src/services/dapp` 继续只承载通用钱包与链交互能力。
- 验证结果：新增 `tests/project-contracts.test.mjs` 约束该目录结构、env key、ABI 方法覆盖和 README 记录。完整 `pnpm lint`、`pnpm test` 和 `pnpm build` 均已通过。

### TRF-032：已登录 DApp 项目应在 App 启动层恢复钱包与全局链上状态

- 状态：计划回灌
- 实战场景：DApp H5 用户可能已经登录并直接刷新或打开 `/h5/home`、`/h5/swap` 等业务页。此时如果钱包、合约实例和全局链上状态只在开屏页登录流程或侧边栏打开时初始化，页面会先拿到空状态，侧边栏邀请链接、全局用户链上身份等状态也会不稳定。
- 发现的问题：`isReferralBound` 这类全局状态不属于侧边栏组件职责。侧边栏只是展示邀请链接，不能因为它打开了才触发合约读取；否则直接刷新首页、先进入其他一级页面或未来新增公共状态时都会重复踩坑。
- 建议方案：模板 DApp 登录模式应提供 App 启动层 bootstrap：应用挂载后检查本地登录态，若处于 DApp 登录模式且检测到钱包环境，则恢复钱包连接、校验缓存地址、启动钱包监听，并立即读取需要全局缓存的链上状态。页面、Header、Sidebar 等组件只消费 store，不主动承担全局初始化。未登录时不强行初始化业务合约，继续由接口 401 或开屏登录流程处理登录。
- 边界约束：App 层只初始化通用钱包会话和已确认的全局状态，不应把某个页面的列表、详情或临时弹窗数据提前加载。合约读取仍通过项目 `services/contracts` wrapper，并保留 `[contract:read]` 诊断日志，方便接口阶段核对链上数据来源。
- 当前项目处理：AIGO 已将 `initializeAuthenticatedDappSession()` 接入 `src/app/App.tsx`，并复用 `resumeDappAuthSession()` 在钱包初始化后同步 `isReferralBound` 到用户 store。`SidebarMenu` 只读取 `isReferralBound` 与钱包地址生成邀请链接，不再承担合约读取职责。
- 验证结果：新增认证启动与邀请状态契约测试，约束 App 层 bootstrap、启动恢复、侧边栏只消费 store，以及合约读取诊断日志。

### TRF-033：全局反馈应提供方法级 Message/Toast 模块

- 状态：计划回灌
- 实战场景：请求非 200、复制成功或失败、合约写入结果、全局校验失败等反馈不一定发生在页面组件内部，服务层、指令式工具和业务 action 都需要能直接触发 toast。
- 发现的问题：如果 toast 只能通过页面挂载组件或 Provider 使用，HTTP 拦截器这类全局基础设施会很难接入；如果继续沿用旧项目 `innerHTML` 拼接 DOM 的方式，又会绕过 React 组件边界，并带来字符串插入风险和样式维护成本。
- 建议方案：模板提供方法级 `Message` 模块，对外暴露 `message()`、`message.success()`、`message.fail()`、`message.warning()`、`message.info()` 和 `message.close()`。内部用单例 root 动态添加到 `document.body`，新消息清理旧计时器并复用同一生命周期，视觉仍由 React 组件和 SCSS 维护。服务模块应在无 `document` 环境下安全 no-op，便于 Node 测试和 SSR 边界。
- 当前项目处理：AIGO 已新增 `src/components/Message`，并在 HTTP 响应错误拦截器里对标准化后的 `HttpError.message` 调用 `message.warning()`。全局样式入口统一加载 Message SCSS，服务层不直接引入 SCSS，避免 Node 测试导入 HTTP client 时触发样式扩展解析。
- 验证结果：新增 `tests/message.test.mjs` 约束方法级 API、单例动态挂载/销毁、无 `innerHTML`、全局样式注册和 HTTP 非 200 message 接入。

### TRF-034：移动端分页应优先提供触底加载组件

- 状态：计划回灌
- 实战场景：AIGO 多个页面列表接口不返回 `total` 或总页数，只能根据本次返回条数是否达到 `page_size` 判断是否还有下一页。传统页码式分页组件在 H5 页面里使用频率较低，触底自动下一页更符合当前项目。
- 建议方案：模板先沉淀轻量 `InfiniteScroll` 组件，只负责监听底部哨兵并在 `loading=false`、`hasMore=true` 时触发 `onLoadMore`；页面或业务 hook 继续负责请求接口、维护页码、拼接列表和判断 `hasMore`。组件不得接收接口地址、列表字段名或请求参数，避免把业务分页协议写死进公共 UI。
- 当前项目处理：首页协作订单列表已接入 `InfiniteScroll`。进行中订单只请求第一页且不继续加载；已完成订单按 `page_no`、`page_size` 请求，并在触底时追加下一页。

### TRF-035：合约写入等待必须覆盖完整交易流程

- 状态：已回灌
- 实战场景：DApp 页面执行入金、提取、兑换等合约操作时，用户需要先在钱包中确认交易，再等待链上回执；部分流程还会连续执行 ERC20 授权和业务合约写入。
- 发现的问题：只给按钮增加禁用态或在单次 `writeContract` 等待时临时展示 loading，无法覆盖 gas 检查、授权额度读取和连续交易之间的间隙。用户会误以为流程结束，重复点击或关闭业务弹窗。
- 建议方案：所有发起写合约的页面和 feature action 都使用共享 `ContractLoading`。等待状态在流程开始前开启，覆盖 gas 检查、授权读取、钱包确认及每笔交易回执；连续写入共用一个状态，直至最后一笔交易成功或失败后才关闭。按钮禁用仅作补充，不能替代全屏阻塞遮罩。若后续刷新依赖链上索引的 API 数据，必须通过 `waitForDappContractDataSync()` 等待 `DAPP_CONFIG.contractWriteRefreshDelayMs` 后再刷新，不能在页面散落定时器。
- 当前项目处理：首页入金将 `homeDepositSubmitting` 贯穿 USDT 授权与绑定下单的整个流程，并接入 `ContractLoading`；订单和 USDT 余额刷新会先等待统一的 `waitForDappContractDataSync()`。`AGENTS.md` 和 `src/services/dapp/README.md` 已将该行为定义为后续写合约功能的强制规范。
- 验证结果：新增文档契约测试，约束协作规则、DApp 接入说明和本反馈条目必须同时保留该规范。

### TRF-036：接口阶段页面余额必须绑定真实业务来源

- 状态：计划回灌
- 实战场景：静态页面开发时常用设计稿金额占位，接口阶段如果只接列表或按钮动作，摘要区余额容易继续显示假数据。用户会把“权重金额”“可用余额”等关键数值当成真实账户状态。
- 发现的问题：权重页摘要金额仍保留静态 `126,567.086748`，但当前业务确认该余额应来自用户信息接口 `/api/users/my` 的 `balance_xo` 字段。
- 建议方案：模板在接口接入阶段应要求每个页面对照设计稿中的余额、收益、额度、进度等关键数字，逐项标注真实来源：用户信息、业务列表汇总、链上合约读取或远程配置。页面不得保留静态金额；真实数据未返回前使用统一占位，返回后通过共享 formatter 渲染。
- 当前项目处理：AIGO 权重页已在挂载时调用 `getCurrentUser()`，读取 `balance_xo` 并通过 `formatAmount()` 展示；接口失败或未返回前显示 `--`。
- 验证结果：更新 `tests/weight-page.test.mjs`，约束权重页必须从用户信息接口读取余额、格式化 `balance_xo`，并禁止静态金额回归。

### TRF-037：通用 Empty 空态文案应统一默认值

- 状态：计划回灌
- 实战场景：列表、选择器、分页流水、订单等多个场景都会出现无数据状态。接口阶段继续让每个调用方传入“暂无订单”“暂无流水”“暂无可选择数据”等细分文案，会让空态表达变得零散，并增加页面接入成本。
- 发现的问题：`Empty` 和 `Picker` 曾开放自定义空态文案参数，业务页面自然会逐个定制。当前项目确认无数据状态统一展示“暂无数据”即可，不需要按场景细分。
- 建议方案：模板通用 `Empty` 组件直接渲染 i18n 默认文案 `暂无数据`，不再提供文案参数；`Picker` 等二次封装组件也只复用默认 `Empty`，不透传业务文案。页面只决定是否显示空态、是否保留默认上下间距和额外 class。
- 当前项目处理：AIGO 已移除 `Empty.text` 与 `Picker.emptyText`，首页订单、权重资产列表、语言切换和 Picker showcase 均改为默认空态。
- 验证结果：更新 Empty、Picker、首页、权重页和语言切换契约测试，约束源码不再传入自定义空态文案，并保留默认 `暂无数据` 展示。

### TRF-038：二级业务页应与一级布局解耦

- 状态：计划回灌
- 实战场景：首页收益卡片中的“查看领取明细”需要进入领取明细页，但该页面不是底部菜单或侧边栏中的一级页面。
- 发现的问题：如果把二级页直接挂在 `MainLayout` 下，页面会同时出现一级页面 Header 和二级页返回 Header；如果把二级页塞进 `MAIN_PAGE_ITEMS`，又会污染一级菜单配置。
- 建议方案：模板应支持“鉴权内、主布局外”的二级路由结构。一级页面继续由 `MainLayout` 和 `MAIN_PAGE_ITEMS` 管理；二级业务页独立使用 `SecondaryHeader`，路由仍受登录鉴权保护。
- 当前项目处理：AIGO 新增 `/home/reward-detail` 领取明细页，首页入口通过 `useAppNavigate()` 跳转；页面复用 `SecondaryHeader`、`SegmentedTabs`、`InfiniteScroll` 和默认 `Empty`，提供“静态收益 / 动态收益”两个筛选项，并通过 `/api/orders/reward_logs` 按 `type` 分页读取数据。
- 验证结果：新增领取明细页契约测试，并更新首页入口与路由配置测试，约束二级路由不进入一级菜单。

### TRF-039：合约 ABI 方法名不能直接决定前台资产动作

- 状态：计划回灌
- 实战场景：同一个业务页可能同时展示 XO、权重等多个资产字段，合约 ABI 中也可能同时存在 `claim`、`claimQuota` 等相近方法。接口阶段如果按方法名猜测业务动作，很容易把“余额展示”和“可提取资产”混在一起。
- 发现的问题：权重 tab 曾按 XO 的模式补了提取弹窗和 `dividend_token` 签名提取，但当前业务确认权重没有提取入口；同时 XO 提取的链上写入应使用最新 ABI 的 `LAXProject.claimQuota(...)`，不是旧的 `claim(...)`。
- 建议方案：页面资产配置应显式标注 `supportsClaim`、流水支持和接口 `ccy`，不要仅因为 ABI 里存在方法就开放 UI 动作。对需要后端签名的提取流程，前台 README 应写清“接口签名参数”和“最终写链方法”的对应关系。
- 当前项目处理：AIGO 权重页保留 XO/权重 tab；切到权重时只展示用户信息接口的 `dividend_token` 余额，不展示提取按钮和流水；XO 提取继续先请求 `/api/claims`，但写链改为 `LAXProject.claimQuota(...)`。
- 验证结果：更新 `tests/weight-page.test.mjs`，约束权重 tab 不暴露提取入口，XO 提取封装不再导入或调用 `writeLaxProjectClaim`，只调用 `writeLaxProjectClaimQuota`。

### TRF-040：页面级下拉刷新应等待真实页面数据完成

- 状态：计划回灌
- 实战场景：移动端 H5 业务页通常都需要下拉刷新，但开屏页、登录页这类流程页不应参与。首页、闪兑、存入、权重、领取明细等页面的数据来源不一致，可能同时包含接口列表、用户信息、链上余额、合约读取和页面定时器。
- 发现的问题：如果只在全局容器里做下拉图标，或页面 handler 只是递增 `refreshVersion` 触发 `useEffect`，刷新组件无法知道真实数据何时结束；如果首页定时器、下拉刷新、入金/提取/领取后的刷新各写一套逻辑，又容易出现重复请求、刷新后旧请求回写和个人中心等公共数据漏刷。
- 建议方案：模板应提供页面级 `PagePullRefresh` 容器和 `usePageRefresh()` 注册 API。已登录业务路由统一包裹容器，开屏/登录等流程页排除；具体页面注册返回 `Promise<void>` 的刷新函数，函数内部直接调用真实数据加载方法并 `await` 完成。页面刷新应使用请求序号或 AbortController 防止旧请求覆盖新状态；有合约写入或页面提交中时临时禁用下拉。对含定时器的页面，手动下拉和写入成功后的刷新都复用同一个整屏刷新函数，刷新期间暂停定时器，完成后再恢复。若刷新函数会在 `finally` 里恢复定时器，必须带页面生命周期 token 或 mounted guard，避免离开页面后旧异步重新创建 interval；如果切换到不展示列表的 tab，也要主动废弃对应列表的旧请求，避免隐藏数据回写。
- 当前项目处理：AIGO 新增 `src/components/PagePullRefresh`，在鉴权后的业务路由外层统一启用下拉刷新。首页的 `refreshHomeScreenData()` 会同时刷新个人中心、USDT 余额、入金范围、动态/静态收益和订单列表，并被初始加载、10 秒定时器、下拉刷新以及入金/领取后的数据同步复用；存入、闪兑、权重和领取明细页也都改为注册可等待的页面刷新函数。首页通过刷新生命周期 token 防止离页后恢复 10 秒定时器；权重 tab 只刷新用户信息余额，不额外请求流水，并会作废 XO tab 遗留的流水请求；XO tab 才刷新余额和第一页流水。
- 验证结果：新增 `tests/page-pull-refresh.test.mjs` 约束交互组件、路由包裹范围和无文案动态图标；更新首页、存入、闪兑、权重、领取明细页面测试，约束下拉刷新必须复用真实数据加载函数并防旧请求回写。完整 `pnpm lint`、`pnpm test` 和 `pnpm build` 均已通过。

### TRF-041：接口联调后应回收页面内的数据编排

- 状态：已回灌
- 实战场景：Figma 静态页在对接接口、合约、定时器、下拉刷新、分页和写入反馈后，很容易把所有状态、请求守卫、刷新函数、表单校验和大段 JSX 都堆在一个页面文件里。
- 发现的问题：AIGO 首页已经承载个人中心刷新、钱包余额、入金限制、动态收益、静态收益、订单分页、进度读取、多个提取弹窗和 10 秒整屏刷新；其他页面也重复出现 `requestRef + isCurrent()` 的旧请求守卫。页面能工作，但继续迭代时维护成本和漏测风险会上升。
- 建议方案：模板应在“接口联调完成复盘”阶段要求做一次页面数据编排回收：把页面数据加载拆成 `use<Page>Data()`，把写入/提交流程拆成 `use<Page>Actions()` 或页面同目录 service；把重复的旧请求守卫沉淀为 `useLatestRequest()` 或 `useAsyncGuard()`；把订单卡片、收益卡片、资产行等重复 JSX 拆成页面内组件。页面 TSX 尽量只保留布局组合和少量事件转发。
- 当前项目处理：AIGO 已新增 `useLatestRequest()` 统一处理页面异步请求过期保护，存入、闪兑、权重和领取明细页不再各自维护 `requestRef + isCurrent()`；首页拆出 `useHomeScreenData()` 负责个人中心、余额、入金限制、动态/静态收益、订单分页、下拉刷新和 10 秒整屏刷新编排，页面只保留表单校验、弹窗和写入入口；首页重复 JSX 已拆为 `HomeCooperationCard`、`HomeIncomeCard` 和 `HomeOrderCard`。进行中订单的进度与可领取收益读取改为按订单并发读取，避免多订单时串行等待。
- 验证结果：新增 `tests/page-request-optimization.test.mjs`，并更新首页、存入、闪兑、权重和领取明细页契约测试，约束公共请求守卫、首页数据 hook、首页卡片组件和订单合约读取并发化。完整 `pnpm test`、`pnpm lint` 和 `pnpm build` 均已通过。

### TRF-042：DApp 已登录启动必须先校验当前钱包账号

- 状态：已回灌
- 实战场景：用户在钱包 DApp 浏览器或桌面 MetaMask 中完成登录后关闭网页，再到钱包里切换账号，随后重新打开项目域名。浏览器仍保留旧 token 和旧缓存 Address，但钱包当前账号已经变更。
- 发现的问题：AIGO 启动层曾在校验当前钱包账号前先调用个人中心 `/api/users/my`，导致请求头继续携带旧 token 和旧 Address；随后恢复 DApp 会话发现缓存地址与当前钱包不一致并执行 `logout()`，但开屏页本轮启动流程已经结束，`hasStartedRef` 阻止再次触发登录，所以页面停在开屏页，只有刷新后才重新弹出钱包登录。
- 建议方案：DApp 登录模式下，存在 token 时必须先检测钱包环境并恢复/校验当前钱包账号，确认缓存 Address 与当前账号一致后才能加载个人中心等鉴权接口。若恢复失败且当前处于开屏登录流程，应清理旧 token 和缓存 Address，并在同一轮流程里直接重新发起 `loginWithDapp()`，不要依赖路由回到开屏页后再次触发 effect。共享的恢复 Promise 必须在 settle 后释放，避免一次 rejected promise 让后续重试继续失败。
- 当前项目处理：AIGO 已将 `initializeAuthenticatedDappSession()` 调整为账号密码模式才先加载个人中心；DApp 模式先 `resumeStoredDappSession()`，成功后再请求 `/my`。开屏页 `startAuthFlow()` 在旧 token 对应的钱包账号变化时，会 `clearAuthSession()` 清理旧态并立即重新调用 `loginWithDapp()`。`resumeStoredDappSessionPromise` 增加 `finally` 释放，`dappLoginAttempt` 的失效逻辑集中到 `resetDappLoginAttempt()`。
- 验证结果：更新 `tests/auth-session.test.mjs`，约束 DApp 启动请求顺序、账号变化后即时重登、恢复 Promise 释放和签名请求失效逻辑。完整验证在本轮修复后执行。

### TRF-043：开屏动画应作为认证启动统一门槛

- 状态：已回灌
- 实战场景：旧项目开屏 logo 有入场动画，用户进入页面后需要先完整展示开屏动画，再进入登录、钱包签名或已登录恢复逻辑。
- 发现的问题：AIGO 曾只在账号密码模式等待开屏动画，DApp 模式会直接检测 Provider 并发起钱包登录；已有 token 的 App 顶层恢复也可能在开屏页动画结束前请求个人中心或恢复钱包会话。
- 建议方案：模板应把开屏动画等待封装为页面级共享工具，开屏页的 `startAuthFlow()` 必须统一等待动画结束；App 顶层的已登录恢复若当前路径仍是开屏页，也应复用同一等待工具，避免旧 token 恢复抢在动画前执行。直接刷新业务页时不额外等待开屏动画。
- 当前项目处理：AIGO 已新增 `src/pages/splash/animation.ts`，统一维护开屏动画时长、等待方法和开屏路由判断；`SplashPage` 与 App 顶层 DApp 会话恢复共同复用该工具。
- 验证结果：更新 `tests/auth-session.test.mjs`，约束开屏页所有认证路径和 App 顶层恢复都必须先经过开屏动画门槛。

## 新反馈模板

### TRF-XXX：标题

- 状态：待评估
- 实战场景：
- 发现的问题：
- 建议方案：
- 当前项目处理：
- 验证结果：
