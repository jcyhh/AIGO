# 首页订单卡片自适应布局设计

## 目标

订单金额、日期、进度和多语言文案不再受左右固定分栏的空间限制。

## 布局

- 所有订单使用纵向信息流，取消金额与进度并列的固定宽度布局。
- 顶部保留“协作额度”和日期的左右对齐。
- 金额独占一行。
- 进度说明独占一行，进度条在其下占满卡片宽度。
- 活动订单在分隔线下保留“可领取 / 领取”操作；已完成订单沿用相同顶部布局但不显示领取区。

## 约束与验证

- 不截断金额、日期、进度值或多语言文案；必要时允许自然换行。
- 不改订单数据、进度计算或领取交互。
- 为卡片结构和全宽进度样式补充回归测试。
